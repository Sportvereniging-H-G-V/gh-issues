{
  "name": "gh-issues-creator",
  "version": "2.0.0",
  "description": "React SPA voor het aanmaken van GitHub issues in Sportvereniging-H-G-V repos",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "worker:dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "react-router-dom": "^7.13.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.4",
    "vite": "^7.3.1",
    "wrangler": "^4.71.0"
  }
}
